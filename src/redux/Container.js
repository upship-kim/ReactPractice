import React from 'react';
import { useDispatch } from 'react-redux';
import Component from './Component';

const dispatch = useDispatch(); 


const Container = () => {
    return <Component increase={} decrease={} number={}/>;
};

export default Container;
