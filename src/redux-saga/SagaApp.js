import React from 'react';
import PostContainer from './containers/PostContainer';

const SagaApp = () => {
    return (
        <div>
            <h1>saga app main</h1>
            <div>
                <PostContainer />
            </div>
        </div>
    );
};

export default SagaApp;
